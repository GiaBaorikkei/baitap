// Tính diện tích hình tam giác

// Biết cạnh đáy (a), chiều cao (h)

triangles();
console.log("Trước khi khai báo hàm");

function triangles(a, h) {
    let s = (a * h) / 2;
    
    console.log("Diện tích tam giác: ", s);
}

// Tại 1 nơi nào đó trong chương trình tương lai

triangles(10,20);


// Tại sao có thể gọi được hàm trước khi khai báo?????